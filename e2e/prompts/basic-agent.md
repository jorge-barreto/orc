Write a file at $ARTIFACTS_DIR/result.txt containing exactly the single word "hello" (lowercase, no quotes, no punctuation). A trailing newline is fine.

Do not write anything else. Do not create other files.
