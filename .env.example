# .env.example — copy to .env and fill in values.
# .env is gitignored; never commit real secrets.
#
# CLAUDE_CODE_OAUTH_TOKEN is required only for agent-phase e2e tests
# (orc-gge.2 and later). The script-phase smoke test (orc-gge.1) does
# not need it.
CLAUDE_CODE_OAUTH_TOKEN=
